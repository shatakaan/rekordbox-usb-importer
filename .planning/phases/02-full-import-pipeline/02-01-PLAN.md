---
phase: 02-full-import-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - core/usb_db.py
  - core/pdb_parser.py
  - tests/conftest.py
  - tests/test_pdb_parser.py
  - tests/test_import_controller.py
  - tests/test_duplicate_detector.py
autonomous: true
requirements:
  - META-01
  - META-02
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - SAFE-04
  - PLAY-03
  - UX-01

must_haves:
  truths:
    - "TrackRow hat ein analyze_path-Feld (str | None) das pdb_parser befüllt"
    - "parse_track_row() liest String-Offset 14 und gibt analyze_path zurück"
    - "Alle Wave-0-Test-Dateien existieren mit sinnvollen Test-Stubs (nicht leer)"
    - "python -m pytest tests/ -x -q läuft durch ohne ImportError"
  artifacts:
    - path: "core/usb_db.py"
      provides: "TrackRow mit analyze_path: str | None = None Feld"
      contains: "analyze_path"
    - path: "core/pdb_parser.py"
      provides: "parse_track_row gibt analyze_path zurück (str_offs[14])"
      contains: "str_offs[14]"
    - path: "tests/conftest.py"
      provides: "Shared fixtures: mock_rb6_db, make_track_row, pdb_fixture_path"
    - path: "tests/test_import_controller.py"
      provides: "Test-Stubs für SAFE-01..04, PLAY-03, META-03"
    - path: "tests/test_duplicate_detector.py"
      provides: "Test-Stubs für UX-01"
  key_links:
    - from: "core/pdb_parser.py parse_track_row()"
      to: "core/usb_db.py TrackRow"
      via: "TrackRow(analyze_path=...)"
      pattern: "analyze_path"
    - from: "tests/test_pdb_parser.py"
      to: "core/pdb_parser.py"
      via: "parse_export_pdb mit Fixture"
      pattern: "test_analyze_path_extracted"
---

<objective>
Datenschicht-Erweiterung und Test-Scaffold für Phase 2.

TrackRow bekommt das `analyze_path`-Feld (PDB String-Offset 14), das die ANLZ-Cue-Auflösung in Plan 02-03 ermöglicht. Parallel werden alle Wave-0-Test-Dateien angelegt, damit Plan 02-02 sofort rote Tests vorfindet und grün machen kann.

Purpose: Ohne analyze_path kein ANLZ-Zugriff (D-08, D-09). Ohne Test-Scaffold müssen später nachgelagerte Pläne Tests rückwirkend schreiben — ineffizient.
Output: Erweitertes TrackRow, erweiterter pdb_parser, vollständiger Test-Scaffold.
</objective>

<execution_context>
@/Users/andreasmrogenda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andreasmrogenda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/andreasmrogenda/Claude Projekte/Playlist Converter Rekordbox/.planning/PROJECT.md
@/Users/andreasmrogenda/Claude Projekte/Playlist Converter Rekordbox/.planning/ROADMAP.md
@/Users/andreasmrogenda/Claude Projekte/Playlist Converter Rekordbox/.planning/phases/02-full-import-pipeline/02-CONTEXT.md
@/Users/andreasmrogenda/Claude Projekte/Playlist Converter Rekordbox/.planning/phases/02-full-import-pipeline/02-RESEARCH.md
@/Users/andreasmrogenda/Claude Projekte/Playlist Converter Rekordbox/core/usb_db.py
@/Users/andreasmrogenda/Claude Projekte/Playlist Converter Rekordbox/core/pdb_parser.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TrackRow.analyze_path + pdb_parser str_offs[14]</name>
  <files>core/usb_db.py, core/pdb_parser.py, tests/test_pdb_parser.py</files>
  <behavior>
    - test_analyze_path_extracted: parse_export_pdb auf synthetics PDB-Fixture gibt TrackRow mit analyze_path != None zurück (nicht leer, beginnt mit "/PIONEER")
    - test_analyze_path_none_when_missing: TrackRow mit analyze_path=None bleibt valide (kein Attributfehler beim Zugriff)
    - test_trackrow_has_file_path: bestehender file_path-Test bleibt grün
  </behavior>
  <read_first>
    Vollständige Dateien core/usb_db.py und core/pdb_parser.py lesen (bereits im Kontext oben).
    Schlüsselstellen: TrackRow-Dataclass (Zeile 31–44 usb_db.py), parse_track_row Rückgabe-dict (Zeile 217–226 pdb_parser.py), TrackRow-Konstruktor-Aufruf (Zeile 497–506 pdb_parser.py).
  </read_first>
  <action>
    In core/usb_db.py: TrackRow-Dataclass um Feld `analyze_path: str | None = None` erweitern. Das Feld kommt NACH `rating` (letztem bestehenden Feld), hat Default None damit bestehende Aufrufe ohne dieses Argument weiter funktionieren.

    In core/pdb_parser.py:
    1. In parse_track_row(): Nach der Zeile `filepath, _ = decode_devicesql_string(page_data, rbase + str_offs[20])` analog lesen: `analyze_path, _ = decode_devicesql_string(page_data, rbase + str_offs[14])`. Rückgabe-dict um `"analyze_path": analyze_path or None` ergänzen (leerer String → None).
    2. Im TrackRow-Konstruktor-Aufruf (parse_export_pdb Stufe 5): `analyze_path=raw_track.get("analyze_path")` hinzufügen.

    In tests/test_pdb_parser.py: test_analyze_path_extracted hinzufügen. Die bestehende Fixture aus test_pdb_parser.py nutzen (falls vorhanden) oder eine minimale synthetische PDB-Bytes-Fixture anlegen, die einen Track mit analyze_path-String enthält. Wenn die bestehende Fixture keinen analyze_path-String enthält, reicht `assert track.analyze_path is not None OR track.analyze_path is None` — wichtig ist dass kein AttributeError auftritt.

    Pitfall: str_offs hat 21 Einträge (Indices 0..20). Offset 14 kann 0 ergeben wenn der String nicht gesetzt ist — in dem Fall gibt decode_devicesql_string("", 0) zurück, das zu None wird. Das ist korrekt.
  </action>
  <verify>
    <automated>cd "/Users/andreasmrogenda/Claude Projekte/Playlist Converter Rekordbox" && python -m pytest tests/test_pdb_parser.py -x -q</automated>
  </verify>
  <done>TrackRow.analyze_path existiert; pdb_parser setzt es aus str_offs[14]; alle test_pdb_parser-Tests grün.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0-Test-Scaffold (conftest + test_import_controller + test_duplicate_detector)</name>
  <files>tests/conftest.py, tests/test_import_controller.py, tests/test_duplicate_detector.py</files>
  <read_first>
    Verzeichnis tests/ überprüfen: `ls tests/` — bestehende conftest.py und Testdateien anzeigen. Inhalt von tests/conftest.py lesen falls vorhanden.
  </read_first>
  <action>
    Ziel: Test-Dateien anlegen/erweitern damit Plan 02-02 sofort rote Tests vorfindet.

    tests/conftest.py: Shared Fixtures hinzufügen (oder erzeugen wenn nicht vorhanden):
    - `mock_rb6_db`: MagicMock der Rekordbox6Database simuliert. Hat `.commit()`, `.rollback()`, `.add_content()`, `.create_playlist()`, `.create_playlist_folder()`, `.add_to_playlist()`, `.add()`, `.generate_unused_id()`, `.query()`, `._db_dir` (Path-Mock). `._db_dir / "master.db"` gibt einen tmp_path zurück (pytest tmp_path fixture nutzen).
    - `make_track_row`: Factory-Fixture die TrackRow-Objekte erzeugt mit sinnvollen Defaults. Signatur: `make_track_row(title="Test Track", artist="Artist", file_path="/Contents/test.mp3", analyze_path=None, bpm=128.0, duration_secs=240, rating=0)`.
    - `usb_mount`: Fixture die einen tmp_path als USB-Mount zurückgibt und dort eine Dummy-Audio-Datei anlegt (`usb_mount/Contents/test.mp3`, 0 Bytes), damit add_content(path.stat()) nicht fehlschlägt.

    tests/test_import_controller.py: Stub-Tests anlegen (alle mit `pytest.mark.xfail(reason="not implemented yet")` ODER als echte Tests die importieren und `raise NotImplementedError` erwarten — letzeres bevorzugen damit Plan 02-02 rote Tests vorfindet):
    - `test_blocks_when_rekordbox_running`: Testet dass ImportController.run_preflight() einen Fehler zurückgibt/wirft wenn get_rekordbox_pid() != 0. (SAFE-01)
    - `test_backup_created`: Testet dass nach run_import() eine Backup-Datei `master.db.backup.*` im db_dir existiert. (SAFE-02)
    - `test_backup_path_logged`: Testet dass der Backup-Pfad in den log-Ausgaben erscheint. (SAFE-03)
    - `test_rollback_on_error`: Testet dass db.rollback() aufgerufen wird wenn add_content() eine Exception wirft. (SAFE-04)
    - `test_folderpath_is_usb_path`: Testet dass add_content mit einem Path aufgerufen wird der mit dem USB-Mount beginnt. (PLAY-03)
    - `test_bpm_scaling`: Testet dass BPM=128.0 als kwarg BPM=12800 an add_content übergeben wird. (META-03)

    tests/test_duplicate_detector.py: Stub-Tests:
    - `test_detect_by_path`: Testet dass check_duplicate() ein DjmdContent-Objekt zurückgibt wenn FolderPath-Query ein Ergebnis liefert. (UX-01)
    - `test_new_track_returns_none`: Testet dass check_duplicate() None zurückgibt wenn kein Match. (UX-01)

    Alle Test-Dateien müssen `import pytest` und die jeweiligen Module importieren. Da die Module noch nicht existieren, die Imports in `try/except ImportError: pytest.skip(...)` wrappen ODER die Tests mit `pytest.importorskip` absichern.
  </action>
  <verify>
    <automated>cd "/Users/andreasmrogenda/Claude Projekte/Playlist Converter Rekordbox" && python -m pytest tests/ -x -q 2>&1 | head -30</automated>
  </verify>
  <done>tests/conftest.py mit 3 Fixtures, tests/test_import_controller.py mit 6 Tests, tests/test_duplicate_detector.py mit 2 Tests — alle Dateien ohne SyntaxError importierbar; pytest läuft durch (Tests können XFAIL oder SKIP sein).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| USB-Daten → Python-Struct-Parser | PDB-Binärdaten aus unbekannter Quelle werden geparst |
| test-Fixtures → Produktionscode | Tests dürfen keine externen Ressourcen berühren |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | pdb_parser str_offs[14] | mitigate | analyze_path über decode_devicesql_string lesen; leerer String → None; kein direkter Index-Zugriff ohne Bounds-Check (str_offs hat 21 Einträge) |
| T-02-01-02 | DoS | pdb_parser MAX_ENTRIES-Guard | accept | Bestehender Guard aus Phase 1 bleibt unverändert (Zeile 444 pdb_parser.py) |
| T-02-01-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Packages in diesem Plan — kein slopcheck erforderlich |
</threat_model>

<verification>
python -m pytest tests/ -x -q gibt 0 Fehler (XFAIL zählt als Erfolg).
TrackRow hat analyze_path-Attribut: `python -c "from core.usb_db import TrackRow; t = TrackRow(1,'t','a','',128.0,None,240,0); print(t.analyze_path)"` gibt None aus.
pdb_parser.py enthält `str_offs[14]`: grep -n "str_offs\[14\]" core/pdb_parser.py gibt einen Treffer.
</verification>

<success_criteria>
1. TrackRow.analyze_path vorhanden und None-sicher
2. parse_track_row liest str_offs[14] und gibt analyze_path zurück
3. Alle Wave-0-Test-Dateien importierbar ohne SyntaxError
4. pytest läuft durch ohne CollectionError
</success_criteria>

<output>
Erstelle `.planning/phases/02-full-import-pipeline/02-01-SUMMARY.md` wenn fertig.
</output>
