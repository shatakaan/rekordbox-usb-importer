---
phase: 02-full-import-pipeline
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - ui/track_panel.py
autonomous: true
requirements:
  - UX-01
  - UX-02
  - SAFE-03

must_haves:
  truths:
    - "TrackPanel kann in einen 'summary'-Modus wechseln (populate_import_summary statt populate)"
    - "Summary-Modus zeigt Spalten: TITLE, ARTIST, BPM, DURATION, STATUS — STATUS-Spalte ganz rechts"
    - "Jede Zeile hat eine Checkbox in Spalte 0 (per D-06: NEW=gecheckt, DUPLICATE=ungecheckt)"
    - "Ein 'Back'-Button erscheint über der Tabelle im Summary-Modus (per D-03)"
    - "restore_browse_mode() schaltet das Panel zur normalen populate()-Ansicht zurück"
    - "get_import_selections() gibt dict[int, bool] zurück (track_id → will_import)"
  artifacts:
    - path: "ui/track_panel.py"
      provides: "TrackPanel.populate_import_summary(), restore_browse_mode(), get_import_selections()"
      exports:
        - "populate_import_summary"
        - "restore_browse_mode"
        - "get_import_selections"
  key_links:
    - from: "ui/track_panel.py populate_import_summary()"
      to: "core/import_controller.ImportPlan"
      via: "plan.track_statuses (dict[int, TrackImportStatus])"
      pattern: "track_statuses"
    - from: "ui/main_window.py (Plan 02-05)"
      to: "ui/track_panel.py"
      via: "track_panel.populate_import_summary(plan) nach Klick auf Import-Button"
      pattern: "populate_import_summary"
---

<objective>
Summary-Panel UI: TrackPanel Mode-Switch für Pre-Import-Bestätigungsansicht.

TrackPanel bekommt einen zweiten Modus der die Trackliste als Pre-Import-Summary anzeigt: STATUS-Spalte, Checkboxen per Track (NEW=gecheckt, DUPLICATE=ungecheckt), Back-Button, Confirm-Import-Button. Kein neues Fenster — ersetzt das rechte Panel (per D-03).

Purpose: Der DJ muss sehen was importiert wird und Duplikate einzeln entscheiden können (D-02, D-06, UX-01, UX-02). Backup-Pfad wird in der Summary oben angezeigt (SAFE-03 per D-14).
Output: Erweitertes ui/track_panel.py mit populate_import_summary, restore_browse_mode, get_import_selections.
</objective>

<execution_context>
@/Users/andreasmrogenda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andreasmrogenda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/andreasmrogenda/Claude Projekte/Playlist Converter Rekordbox/.planning/phases/02-full-import-pipeline/02-CONTEXT.md
@/Users/andreasmrogenda/Claude Projekte/Playlist Converter Rekordbox/.planning/phases/02-full-import-pipeline/02-RESEARCH.md
@/Users/andreasmrogenda/Claude Projekte/Playlist Converter Rekordbox/ui/track_panel.py
@/Users/andreasmrogenda/Claude Projekte/Playlist Converter Rekordbox/.planning/phases/02-full-import-pipeline/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TrackPanel Summary-Modus (populate_import_summary, restore_browse_mode, get_import_selections)</name>
  <files>ui/track_panel.py</files>
  <read_first>
    Vollständige Datei ui/track_panel.py lesen (bereits im Kontext verfügbar).
    Schlüsselstellen: __init__ (Spalten-Setup, table-Referenz), populate() (Muster für Tabellenbefüllung), COLUMNS-Konstante.
    02-CONTEXT.md D-01, D-02, D-03, D-06 (Summary-UX-Spezifikation).
    core/import_controller.py (TrackImportStatus-Enum: NEW, DUPLICATE, SKIP; ImportPlan-Dataclass — aus Plan 02-02-SUMMARY lesen).
  </read_first>
  <action>
    ui/track_panel.py erweitern ohne bestehenden Code zu beschädigen:

    Neue Konstante `SUMMARY_COLUMNS = ["", "TITLE", "ARTIST", "BPM", "DURATION", "STATUS"]` (leerer String für Checkbox-Spalte 0).

    Im __init__: Instanz-Variable `self._mode = "browse"` hinzufügen. QVBoxLayout-Referenz speichern als `self._layout = layout`. Einen `self._summary_header = QWidget()` als Container für Back-Button und Backup-Info-Label anlegen. Initial versteckt: `self._summary_header.setVisible(False)`.

    Summary-Header-Widget aufbauen:
    - `_summary_layout = QHBoxLayout(self._summary_header)` mit Margins (8, 4, 8, 4).
    - `self._backup_label = QLabel("")` — zeigt Backup-Pfad-Info an.
    - `self._back_btn = QPushButton("Back")` mit objectName "secondaryBtn".
    - `self._confirm_btn = QPushButton("Confirm Import")` mit objectName "primaryBtn".
    - Layout: back_btn + stretch + backup_label + stretch + confirm_btn.
    - self._summary_header oben im Layout einfügen: `self._layout.insertWidget(0, self._summary_header)` (vor der Tabelle).

    Methode `populate_import_summary(self, plan, backup_path_str: str = "") -> None`:
    - `self._mode = "summary"`.
    - `self._summary_header.setVisible(True)`.
    - Backup-Label aktualisieren: `self._backup_label.setText(f"Backup: {backup_path_str}" if backup_path_str else "")`.
    - Tabelle auf Summary-Spalten umschalten: `self.table.setColumnCount(len(SUMMARY_COLUMNS))`, `self.table.setHorizontalHeaderLabels(SUMMARY_COLUMNS)`.
    - Spalten-Resize: Spalte 0 = Fixed 32px (Checkbox), Spalte 1 = Stretch (Titel), Spalten 2/3/4 = Interactive min 100px, Spalte 5 = Fixed 90px (STATUS).
    - `self.table.setSortingEnabled(False)` (Status-Sortierung deaktivieren für klare Reihenfolge).
    - `self.table.setRowCount(0)`.
    - Alle Tracks aus plan iterieren: `for track_id, status in plan.track_statuses.items()`. TrackRow aus plan.pdb_db.tracks[track_id] holen (ImportPlan muss .tracks haben — track_id → TrackRow Mapping). Alternativ: populate_import_summary bekommt zusätzlich `tracks: dict[int, TrackRow]` Parameter.
    - Pro Zeile: Zeile einfügen, Spalte 0 = QCheckBox-Item (per D-06: NEW → Qt.CheckState.Checked, DUPLICATE → Qt.CheckState.Unchecked, SKIP → Qt.CheckState.Unchecked + disabled). Spalte 1 = Titel-Text. Spalte 2 = Artist. Spalte 3 = BPM (int). Spalte 4 = Duration (M:SS). Spalte 5 = Status-Text (NEW/DUPLICATE/SKIP), farbig: NEW = grün, DUPLICATE = orange, SKIP = grau.
    - `self.table.setItem(row, 0, checkbox_item)` — QTableWidgetItem mit Qt.ItemFlag.ItemIsUserCheckable | Qt.ItemFlag.ItemIsEnabled, setData(Qt.ItemDataRole.UserRole, track_id).

    Methode `get_import_selections(self) -> dict[int, bool]`:
    - Iteriert alle Zeilen der Tabelle im Summary-Modus.
    - `track_id = self.table.item(row, 0).data(Qt.ItemDataRole.UserRole)`.
    - `will_import = self.table.item(row, 0).checkState() == Qt.CheckState.Checked`.
    - Gibt `{track_id: will_import}` zurück.

    Methode `restore_browse_mode(self) -> None`:
    - `self._mode = "browse"`.
    - `self._summary_header.setVisible(False)`.
    - Tabelle auf browse-Spalten zurücksetzen: COLUMNS + resize-Modus wie im __init__ (Spalte 0 = Stretch, Spalten 1/2 = Interactive, Spalten 3/4/5/6 = Fixed).
    - `self.table.setColumnCount(len(COLUMNS))`, `self.table.setHorizontalHeaderLabels(COLUMNS)`.
    - `self.table.setRowCount(0)`.

    Signals: `self._back_btn.clicked` und `self._confirm_btn.clicked` als öffentliche Qt-Signals zugänglich machen: Nicht direkt verdrahten (das macht MainWindow in Plan 02-05). `self.back_clicked = self._back_btn.clicked` und `self.confirm_clicked = self._confirm_btn.clicked` als Aliase.

    Import: QCheckBox nicht nötig — QTableWidgetItem mit ItemIsUserCheckable reicht für Checkbox-Darstellung in Tabelle. QPushButton, QHBoxLayout bereits importiert? Nein: Imports erweitern um QPushButton, QHBoxLayout.
  </action>
  <verify>
    <automated>cd "/Users/andreasmrogenda/Claude Projekte/Playlist Converter Rekordbox" && python -c "from ui.track_panel import TrackPanel; print('TrackPanel import OK'); from PySide6.QtWidgets import QApplication; import sys; app = QApplication.instance() or QApplication(sys.argv); p = TrackPanel(); print('has populate_import_summary:', hasattr(p, 'populate_import_summary')); print('has restore_browse_mode:', hasattr(p, 'restore_browse_mode')); print('has get_import_selections:', hasattr(p, 'get_import_selections'))"</automated>
  </verify>
  <done>TrackPanel importierbar; populate_import_summary, restore_browse_mode, get_import_selections existieren; _summary_header Widget vorhanden; COLUMNS-Modus unverändert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ImportPlan.track_statuses → UI-Darstellung | Daten aus Core-Layer werden in UI gerendert |
| QTableWidgetItem UserRole data → track_id | track_id wird in Widget gespeichert und von get_import_selections() ausgelesen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Information Disclosure | backup_label zeigt Pfad | accept | Backup-Pfad liegt in ~/Library/Pioneer/rekordbox/ — zeigt keinen sensiblen Inhalt, nur Pfad-String |
| T-02-04-02 | Tampering | get_import_selections UserRole data | accept | track_id ist ein Integer aus lokalem ImportPlan — kein externer Input, keine Injection-Gefahr |
| T-02-04-SC | Tampering | pip installs | accept | Keine neuen Packages |
</threat_model>

<verification>
python -c "from ui.track_panel import TrackPanel; ..." gibt 3x "True" für die drei neuen Methoden.
grep -n "populate_import_summary" ui/track_panel.py gibt mindestens 1 Treffer.
grep -n "restore_browse_mode" ui/track_panel.py gibt mindestens 1 Treffer.
grep -n "_summary_header" ui/track_panel.py gibt mindestens 2 Treffer.
Bestehende Tests: python -m pytest tests/ -x -q gibt 0 Fehler (TrackPanel-Änderungen dürfen keine bestehenden Tests brechen).
</verification>

<success_criteria>
1. populate_import_summary schaltet Tabelle auf 6-Spalten-Summary-Modus
2. NEW-Tracks erscheinen gecheckt, DUPLICATE ungecheckt (per D-06)
3. get_import_selections gibt Mapping track_id → bool zurück
4. restore_browse_mode schaltet zurück auf 7-Spalten-Browse-Modus
5. Back-Button und Confirm-Button als click-Signals zugänglich
</success_criteria>

<output>
Erstelle `.planning/phases/02-full-import-pipeline/02-04-SUMMARY.md` wenn fertig.
</output>
