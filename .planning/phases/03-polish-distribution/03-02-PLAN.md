---
phase: 03-polish-distribution
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - build/app.spec
  - build/dmgbuild_settings.py
  - build/build.sh
autonomous: false
requirements: [DIST-01, DIST-02]
user_setup:
  - service: macOS codesigning
    why: "Ad-hoc signing is applied automatically by PyInstaller — no developer account needed. Recipient must right-click → Open on first launch."
    env_vars: []
    dashboard_config:
      - task: "First-launch Gatekeeper bypass"
        location: "On the recipient Mac: right-click PlaylistConverter.app → Open → click Open in dialog"

must_haves:
  truths:
    - "dist/PlaylistConverter.app builds successfully from build/app.spec"
    - "codesign --verify dist/PlaylistConverter.app exits 0 (ad-hoc signature valid)"
    - "dist/PlaylistConverter-1.0.0.dmg contains PlaylistConverter.app and an Applications symlink"
    - "DMG mounts cleanly and app launches on the build machine after right-click → Open"
    - "CFBundleIdentifier in built .app is com.inevents-mainz.playlist-converter"
  artifacts:
    - path: "build/app.spec"
      provides: "Updated PyInstaller spec with correct bundle ID and pyrekordbox hiddenimports"
      contains: "com.inevents-mainz.playlist-converter"
    - path: "build/dmgbuild_settings.py"
      provides: "dmgbuild configuration for drag-to-Applications DMG"
      contains: "Applications"
    - path: "build/build.sh"
      provides: "Reproducible one-command build script: pyinstaller → verify → dmgbuild"
      contains: "dmgbuild"
  key_links:
    - from: "build/build.sh"
      to: "dist/PlaylistConverter.app"
      via: "pyinstaller build/app.spec"
      pattern: "pyinstaller"
    - from: "build/build.sh"
      to: "dist/PlaylistConverter-1.0.0.dmg"
      via: "dmgbuild -s build/dmgbuild_settings.py"
      pattern: "dmgbuild"
---

<objective>
Update the PyInstaller spec with the correct bundle identifier and complete hiddenimports,
create a dmgbuild settings file, write a reproducible build script, execute the build,
and verify the resulting .app and .dmg are functional.

Purpose: Any DJ should be able to receive the .dmg, drag it to Applications, right-click
→ Open on first launch, and use the app without installing anything.

Output:
- Updated `build/app.spec` (bundle ID per D-04, pyrekordbox submodule hiddenimports per D-08)
- New `build/dmgbuild_settings.py` (drag-to-Applications layout per D-07)
- New `build/build.sh` (deterministic build sequence)
- `dist/PlaylistConverter.app` and `dist/PlaylistConverter-1.0.0.dmg`
- Human checkpoint to verify the .app launches and DMG contents are correct
</objective>

<execution_context>
@/Users/andreasmrogenda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andreasmrogenda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-polish-distribution/03-CONTEXT.md
@build/app.spec
@.planning/phases/03-polish-distribution/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update app.spec + create dmgbuild_settings.py + build.sh (DIST-01, DIST-02, per D-04, D-05, D-07, D-08)</name>
  <files>build/app.spec, build/dmgbuild_settings.py, build/build.sh</files>
  <action>
    Edit `build/app.spec` — make these changes only, leave everything else intact:

    1. Replace `bundle_identifier='com.example.playlist-converter'` with
       `bundle_identifier='com.inevents-mainz.playlist-converter'` (per D-04)

    2. Replace `'LSMinimumSystemVersion': '14.0'` with `'LSMinimumSystemVersion': '13.0'`

    3. After the existing `hiddenimports += ['sqlcipher3', 'pyrekordbox']` line, add
       a new block with explicit pyrekordbox submodules and transitive dependencies:

       hiddenimports += [
           'sqlcipher3.dbapi2',
           'pyrekordbox.anlz',
           'pyrekordbox.anlz.file',
           'pyrekordbox.anlz.structs',
           'pyrekordbox.anlz.tags',
           'pyrekordbox.config',
           'pyrekordbox.db6',
           'pyrekordbox.db6.database',
           'pyrekordbox.db6.tables',
           'pyrekordbox.db6.registry',
           'pyrekordbox.db6.smartlist',
           'pyrekordbox.utils',
           'pyrekordbox.mysettings',
           'pyrekordbox.mysettings.file',
           'packaging',
           'packaging.version',
       ]

    4. Keep `collect_all('PySide6')` unchanged (per D-05 — no size reduction in Phase 3).

    5. Keep `target_arch=None`, `CFBundleShortVersionString: '0.1.0'`, `NSHighResolutionCapable: True`
       all unchanged.

    Create `build/dmgbuild_settings.py` as a new file (dmgbuild Python settings format,
    cited from github.com/dmgbuild/dmgbuild):

    The file must define: `application`, `appname`, `files`, `symlinks`, `icon_locations`,
    `background`, `window_rect`, `icon_size`, `text_size`, `format`.

    - `application = defines.get('app', '../dist/PlaylistConverter.app')`
    - `appname = 'PlaylistConverter'`
    - `files = [application]`
    - `symlinks = {'Applications': '/Applications'}`
    - `icon_locations = {'PlaylistConverter.app': (150, 160), 'Applications': (350, 160)}`
    - `background = 'builtin-arrow'`
    - `window_rect = ((100, 100), (500, 320))`
    - `icon_size = 80`
    - `text_size = 12`
    - `format = 'UDZO'`

    Create `build/build.sh` as a new file. Make it executable (chmod +x is done in the
    script header comment — note: the Write tool creates files without exec bit; the
    executor must run `chmod +x build/build.sh` before running it). Content:

    #!/bin/bash
    set -e
    # Playlist Converter — reproducible build script
    # Usage: bash build/build.sh
    # Output: dist/PlaylistConverter.app, dist/PlaylistConverter-1.0.0.dmg

    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    PROJECT_DIR="$(dirname "$SCRIPT_DIR")"

    cd "$PROJECT_DIR"
    source .venv/bin/activate

    echo "=== Step 1: PyInstaller ==="
    pyinstaller build/app.spec

    echo "=== Step 2: Verify ad-hoc signature ==="
    codesign --verify --verbose dist/PlaylistConverter.app
    echo "Signature OK"

    echo "=== Step 3: dmgbuild ==="
    dmgbuild \
      -s build/dmgbuild_settings.py \
      -D app=dist/PlaylistConverter.app \
      "Playlist Converter" \
      dist/PlaylistConverter-1.0.0.dmg

    echo "=== Build complete ==="
    echo "  App:  dist/PlaylistConverter.app"
    echo "  DMG:  dist/PlaylistConverter-1.0.0.dmg"
    du -sh dist/PlaylistConverter.app dist/PlaylistConverter-1.0.0.dmg

    After writing all three files, run the full build:
    cd "/Users/andreasmrogenda/Claude Projekte/Playlist Converter Rekordbox" && chmod +x build/build.sh && bash build/build.sh 2>&1

    If the build fails at the codesign step with a non-zero exit: the ad-hoc signature
    may have been invalidated by manual file edits. Run
    `codesign --sign - --force --deep dist/PlaylistConverter.app` to re-apply it (see
    RESEARCH Pitfall 3 — use --deep only as last resort here since there are no nested
    bundles that would be damaged in this project's case).

    If dmgbuild exits with "No module named dmgbuild": run
    `pip install dmgbuild==1.6.7` (already confirmed installed per RESEARCH Environment table).
  </action>
  <verify>
    <automated>cd "/Users/andreasmrogenda/Claude Projekte/Playlist Converter Rekordbox" &amp;&amp; codesign --verify --verbose dist/PlaylistConverter.app 2>&amp;1 &amp;&amp; echo "---" &amp;&amp; ls -lh dist/PlaylistConverter-1.0.0.dmg 2>&amp;1 &amp;&amp; echo "---" &amp;&amp; defaults read dist/PlaylistConverter.app/Contents/Info.plist CFBundleIdentifier 2>&amp;1</automated>
  </verify>
  <done>
    - codesign --verify exits 0
    - dist/PlaylistConverter-1.0.0.dmg exists (non-zero size)
    - defaults read CFBundleIdentifier returns "com.inevents-mainz.playlist-converter"
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — .app launches + DMG drag-to-Applications (DIST-01, DIST-02)</name>
  <what-built>
    Task 1 produced:
    - dist/PlaylistConverter.app (657 MB, ad-hoc signed, CFBundleIdentifier=com.inevents-mainz.playlist-converter)
    - dist/PlaylistConverter-1.0.0.dmg (compressed, with Applications symlink)
    The app includes the Phase 3 post-import summary panel and QSettings session persistence.
  </what-built>
  <how-to-verify>
    Step 1 — Verify DMG contents:
      Open dist/PlaylistConverter-1.0.0.dmg in Finder.
      Confirm: PlaylistConverter.app icon and an Applications symlink are both visible
      in the DMG window. The arrow background should be visible.

    Step 2 — Test drag-to-Applications:
      Drag PlaylistConverter.app from the DMG to the Applications symlink (or /Applications directly).
      If macOS shows a Gatekeeper dialog "cannot be opened because the developer cannot be verified":
      right-click the .app → Open → click Open in the secondary dialog.
      Confirm: app opens without crashing.

    Step 3 — Verify Phase 3 features in the installed copy:
      a. Connect Rekordbox USB. Select 1+ playlists, click Import, confirm.
         Expected: post-import summary table appears (PLAYLIST / IMPORTED / SKIPPED / FAILED columns).
      b. Click Done. Quit app. Relaunch from Applications.
         Expected: same USB auto-selected, same playlists pre-checked.

    Step 4 — Verify bundle identity:
      In Terminal:
        codesign --display --verbose=2 /Applications/PlaylistConverter.app 2>&1 | grep -E "Identifier|adhoc"
      Expected output contains: "Identifier=com.inevents-mainz.playlist-converter" and "adhoc"

    Report any crashes, missing UI elements, or verification failures.
  </how-to-verify>
  <resume-signal>
    Type "verified" if all 4 steps pass. Describe any failures with step number and error text.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI / pip install (dmgbuild) | dev-time dependency; runs on developer machine, not in bundle |
| dist/PlaylistConverter-1.0.0.dmg → user's Mac | Downloaded file; ad-hoc signed only; Gatekeeper warns on first open |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Repudiation | DMG download source | accept | Ad-hoc signing only; notarization deferred (no Apple Developer account). Document right-click → Open workaround. SHA256 checksum in release notes is recommended but not blocking. |
| T-03-02-02 | Tampering | dist/PlaylistConverter.app post-build | mitigate | Never edit files inside .app after build — invalidates ad-hoc signature. Rebuild via build.sh if any change is needed (see RESEARCH Pitfall 3). |
| T-03-02-SC | Tampering | dmgbuild PyPI package | accept | Package Legitimacy Audit in RESEARCH confirms dmgbuild 1.6.7 slopcheck [OK]; dev-only dependency, not bundled into .app. |
</threat_model>

<verification>
After human checkpoint passes:

```bash
cd "/Users/andreasmrogenda/Claude Projekte/Playlist Converter Rekordbox"
# Confirm bundle ID in shipped .app
defaults read dist/PlaylistConverter.app/Contents/Info.plist CFBundleIdentifier
# Expected: com.inevents-mainz.playlist-converter

# Confirm DMG is readable
hdiutil verify dist/PlaylistConverter-1.0.0.dmg
# Expected: exit 0

# Confirm app is ad-hoc signed
codesign --display dist/PlaylistConverter.app 2>&1 | grep Signature
# Expected: Signature=adhoc
```
</verification>

<success_criteria>
- build.sh runs to completion without errors
- codesign --verify exits 0 on dist/PlaylistConverter.app
- dist/PlaylistConverter-1.0.0.dmg mounts and contains both .app and Applications symlink
- CFBundleIdentifier = com.inevents-mainz.playlist-converter (per D-04)
- LSMinimumSystemVersion = 13.0 in Info.plist
- Human verifier confirms: app opens after right-click → Open, post-import summary displays,
  session restores on relaunch
</success_criteria>

<output>
Create `.planning/phases/03-polish-distribution/03-02-SUMMARY.md` when done.
</output>
